/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "fakestoreapi.com",
            "webdeasy.de"
        ]
    }
};

export default nextConfig;
